// 

//import necessary files
import java.io.*;
import java.util.scanner;


public class mainIO {
	
	public static void main (String[] args) {
	
		BufferedReader fileInput = new BufferedReader(new FileReader(args[0]));
		BufferedWriter fileOutput = new BufferedWriter(new FileWriter(args[1]));
		Scanner fileScanner = new Scanner(fileInput);
		
		
	while (fileScanner.haxNext()) {
		nextLine = fileScanner.nextLine();
		
		if (nextLine = "\n"){
			//do nothing
		}
		
		else-if (nextLine = "forced partial assingnment:") {
			// forcced partial assingment initialization
			//read the next line
			//assign to linked lidst 
			if (nextLine = "\n") {
				break;
			}
		}
	}
		}
	
	}
}